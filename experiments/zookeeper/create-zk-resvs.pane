root.
reserve(dstHost=10.100.0.3, srcPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.4, srcPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.8, srcPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.9, srcPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.10, srcPort=2888) = 200 on rootShare from now to +300.

reserve(dstHost=10.100.0.3, dstPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.4, dstPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.8, dstPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.9, dstPort=2888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.10, dstPort=2888) = 200 on rootShare from now to +300.

reserve(dstHost=10.100.0.3, srcPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.4, srcPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.8, srcPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.9, srcPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.10, srcPort=3888) = 200 on rootShare from now to +300.

reserve(dstHost=10.100.0.3, dstPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.4, dstPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.8, dstPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.9, dstPort=3888) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.10, dstPort=3888) = 200 on rootShare from now to +300.

reserve(dstHost=10.100.0.2, srcPort=2181) = 200 on rootShare from now to +300.

reserve(dstHost=10.100.0.3, dstPort=2181) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.4, dstPort=2181) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.8, dstPort=2181) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.9, dstPort=2181) = 200 on rootShare from now to +300.
reserve(dstHost=10.100.0.10, dstPort=2181) = 200 on rootShare from now to +300.
