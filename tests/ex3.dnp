True  <- root : AddUser Arjun.
True  <- root : NewShare net0 (root,Arjun) reserve(*) <= 1600 on rootShare. // physical max

True  <- root : NewShare arjunShare (Arjun) reserve(user=Arjun) <= 1000 on net0.
True  <- root : GrantUse Arjun on arjunShare.

True  <- Arjun : reserve(user=Arjun, app=1) = 300 on arjunShare. // valid
True  <- Arjun : reserve(user=Arjun, app=2) = 400 on arjunShare. // valid
False <- Arjun : reserve(user=Arjun, app=3) = 400 on arjunShare. // invalid ... exceeds mask
True  <- root : reserve(user=Arjun, app=3) = 400 on net0. // valid
False <- root : reserve(user=Arjun, app=3) = 600 on net0. // invalid ... exceeds physical max
True  <- root : reserve(user=Arjun, app=3) = 500 on net0. // valid

// reserve(user={Arjun,Adf}
// reserve(user=Arjun, user=Adf)

// root : latency(user=Arjun, app=3) = 20 on net0.
// root : allow(user=Arjun) on net0.


